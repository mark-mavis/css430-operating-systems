#include <unistd.h>
#include <sys/wait.h>
#include <stdio.h>
#include <stdlib.h>


int main(){
    /*
    while (true){
        // Produce an item in next_produced 

        wait(empty);
        wait(mutex);

        // add next_produced to the buffer 

        signal(mutex);
        signal(full);
    }

    */

    return 0;
}